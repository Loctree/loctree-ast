# MATRIX P0 control map

`[x]` means the regression is exercised by `run.sh` against the selected binary
bundle. `[?]` means the remaining claim depends on an operator machine or an
external repository and must not be inferred from a green local pack.

| Probe | Local product control | Status |
|---|---|---|
| LCT-A04 | Literal results expose a complete, internally consistent file universe; common-token output is not presented as an unqualified oracle count. | [x] |
| LCT-A06 | Phrase and asset tokens are discovered across classified surfaces; `find --discover --limit 2` enforces a global two-result page. | [x] |
| LCT-A07 | Literal output carries scan completeness, indexed/scanned totals, exclusions, and pagination rather than a polished count without provenance. | [x] |
| LCT-B03 | A file-qualified Python symbol returns one full indentation extent with `truncated=false` only when the return line is present. | [x] |
| LCT-C01 | A binary entrypoint with zero indexed consumers is `[UNPROVEN]`; the output must not contain `Safe to remove`. | [x] |
| LCT-D02 | After a fixture HEAD move, `--no-scan` is explicitly stale and `--fresh` produces a receipt tied to live HEAD plus runtime inventory. | [x] |
| LCT-E01 | `doctor --cache --scope --json` reports schema 1.2, complete project-local enumeration under an isolated cache. | [x] |
| LCT-G01 | A Swift `@main` application is not high-confidence dead while a dormant control symbol is still detected. | [x] |
| LCT-L03 | `loct`, `loctree`, and `loctree-mcp` from the selected directory share schema, bundle ID, and commit. | [x] |
| LCT-Z01 | End-state integrity across independently moving repositories and concurrent processes. | [?] external multi-repo audit |

## Residual operator probes

- [?] Replay the original A04/A07 oracle counts in the original external repos
  if historical number-for-number comparison is required. The local control
  proves honest accounting, not frozen third-party corpus contents.
- [?] Measure LCT-E01 peak RSS against the operator's real legacy/global cache.
  The local control proves the product selects project-local enumeration.
- [?] LCT-L03 remains installed-runtime pending until the operator performs the
  atomic bundle switch and restarts MCP clients.
- [?] LCT-Z01 remains external by definition; record repository heads and
  process/binary markers during the independent end-state audit.
